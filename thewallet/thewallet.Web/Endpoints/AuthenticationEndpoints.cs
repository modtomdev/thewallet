namespace thewallet.Web.Endpoints;

public static class AuthenticationEndpoints
{
    /*
    public static IEndpointRouteBuilder MapAuthenticationEndpoints(this IEndpointRouteBuilder route)
    {
        var endpoints = route.MapGroup("/api/auth");
        //.RequireAuthorization()
        route.MapPost("/login", Login);
        route.MapPost("/register", Register);
        return route;
    }
    private static async Task Login(IAuthenticationService data, LoginRequest request)
    {
        await data.LoginAsync(request);
    }
    private static async Task Register(IAuthenticationService data, RegisterRequest request)
    {
        await data.RegisterAsync(request);
    }*/
}
